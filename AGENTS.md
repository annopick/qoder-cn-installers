## Agent skills

### Issue tracker

Issues live as GitHub Issues (via `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

使用默认五标签分流体系（needs-triage → needs-info → ready-for-agent → ready-for-human → wontfix）。See `docs/agents/triage-labels.md`.

### Domain docs

Single-context 仓库：根目录 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`.
